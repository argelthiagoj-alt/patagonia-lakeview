import "server-only";

/**
 * Favorites service — PLACEHOLDER.
 *
 * Se completa cuando el repo tenga las funciones definitivas (ver
 * `repo.ts`). El contract previsto es:
 *
 *   toggleFavorite(user, cabinId) →
 *     | { ok: true; favored: boolean }
 *     | { ok: false; reason: "NOT_FOUND" | "BANNED" | "SERVER" }
 *
 *   listFavorites(userId) →
 *     | { ok: true; items: CabinView[] }
 *     | { ok: false; reason: "SERVER" }
 */
export {};
