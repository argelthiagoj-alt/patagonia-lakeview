import "server-only";

/**
 * Demo-tools service — PLACEHOLDER.
 *
 * Futuro alcance:
 *   - resetDemoReservations()
 *   - resetDemoUsers()
 *   - clearSimulatedPayments()
 *
 * Hoy solo existe `config.ts` que centraliza la whitelist y el flag.
 */
export {};
